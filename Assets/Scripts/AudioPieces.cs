using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AudioPieces : MonoBehaviour
{
    private bool isDragging = false;

    private Vector2 offset, originalPos;

    // Update is called once per frame
    void Update()
    {
        if(!isDragging) 
        {
            return;
        }

        
    }
}
